{
  "name": "cs2-rank-webpage",
  "version": "1.0.0",
  "description": "",
  "main": "fiveserver.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css:dev": "npx tailwindcss -i input/style.css -o src/style.css --watch",
    "build:css": "npx tailwindcss -i input/style.css -o src/style.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.5"
  }
}